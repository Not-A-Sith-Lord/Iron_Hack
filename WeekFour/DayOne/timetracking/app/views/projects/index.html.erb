<h3>DB Wizardry</h3>
<ul>
<% @projects.each do |project| %>
	<li><%= link_to project.name, project_path(project) %>
		<% if project.description %>
			- <%=project.description %>
			<% end %>

	</li>
	
	<% end %>
</ul>

